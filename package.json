{
  "name": "pixi-game-template",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "start": "yarn clear && parcel index.html --no-hmr",
    "build": "yarn clear && parcel build index.html",
    "clear": "rm -rf dist && rm -rf .parcel-cache"
  },
  "devDependencies": {
    "parcel": "^2.0.1",
    "parcel-reporter-static-files-copy": "^1.3.1",
    "typescript": "^4.5.2"
  },
  "dependencies": {
    "pixi.js": "^6.2.0"
  }
}
